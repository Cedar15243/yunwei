# V7.3.28 语音状态与现场证据 QA 记录

**日期：** 2026-07-25  
**版本：** `7328 / 7.3.28-voice-status-evidence-qa`  
**包名：** `com.codex.air3nativecamera.dingdangexpert.follow.preview.v734taskqa`

## 调整

- AI Agent 中心改为单行三列结构，首轮监听页移除遗留固定最小高度，两类页面在 Air3 `1280x720` CSS 视口中均不再产生第二屏。
- 修复 `photoDraft` 被 HUD 安全状态过滤为首页的问题；拍照后可见缩略图、30 秒等待、语音补充和仅图发送提示。
- 系统语音恢复提示不再覆盖用户问题、现场图片和 AI 回复。
- 任务对话继续保持全屏单页：用户问题、图片证据、AI 当前回复和分页控制在同一视口中；专家退出后恢复原任务证据。

## 自动化与构建

- Android 单测 `74/74`；专家服务 `21/21`；专家网页 `28/28`。
- `npm run validate:native-hud`、`npm run validate:native-build` 通过。
- Gradle `testDebugUnitTest assembleDebug` 通过；APK v2 签名通过。
- 公网专家协同健康检查 `https://bb.chinacedar.top:2305/health` 返回 `200`。
- 构建仍有旧版 D8 对 API 34 的兼容性警告，但测试、编译和打包成功。

## Air3 真机验证

- 最终 APK 已覆盖安装到 `YM00FCF3NW0031`，设备内文件与归档 APK 的 SHA256 均为 `8FAECFB49B4A3A30EFD419877F487E23C124D87A4F11C9BB4974237B62C9A0E1`。
- 冷启动 `1161ms`，低于 `<3s` 目标；Crash buffer 为空。
- 首页静置 20 秒未自行进入监听；显式启动无声监听后约 1.5 秒结束，显示“没有听清”，不调用 AI，并恢复离线唤醒。
- 现场实拍后进入照片草稿，页面严格单屏；30 秒无描述后自动仅图发送，AI 对模糊画面要求补拍，没有伪造确定诊断，返回后仍在任务对话页。
- 专家等待页可正常建立呼叫；硬件返回后恢复原任务、图片和 AI 回复。最终安装包再次验证专家入口和退出无崩溃。
- 能力中心九项 3x3 单屏显示；`AI Agent 中心 -> 能力中心 -> 首页` 逐级返回正确。
- 长回复第 `1/3`、`2/3` 页均保持 `720/720px`，切页只替换正文，不跳监听页。
- 60 秒稳定性采样中 PID 不变，PSS `164MB -> 162MB`，Thermal Status 始终为 `0`，电池温度 `27.0C`；系统启动原因是 `shutdown,userrequested`，未发现 App Crash、ANR、OOM 或触发关机的证据。
- APK：`output/apk/dingdang-v7328-voice-status-evidence-qa.apk`。
- 真机截图：`output/air3-qa/v7328-final/`。

## 人工验收边界

- 真人“小叮当”一遍命中率、连续两次唤醒、分页后再次唤醒、环境误触发和真实语音转写准确率。
- 真人照片加语音联合 AI 回答及长任务第 10、20 轮的语义记忆正确性。
- 真实专家接听后的双向音视频、弱网重连和挂断听感。
- 20 分钟持续运行、佩戴温升和自动关机专项测试；本次 60 秒采样不能替代该验收。
