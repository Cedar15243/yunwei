# V7.3.1 QA Verification Snapshot

**Date:** 2026-07-23
**Scope:** Local operations modules, lifecycle stability and offline wake deployment verification.
**QA package installed on Air3:** `com.codex.air3nativecamera.dingdangexpert.follow.preview.v73uiaudit`
**QA version:** `7302 / 7.3.1-voice-enabled`

## Scope Retained

- Existing diagnosis, realtime ASR, AI request, photo upload and TRTC contracts were not changed by the local operations modules.
- Newly exposed local capability pages are limited to task evidence, local inspection, device profile, SOP catalog, Skill catalog and Agent catalog. They do not claim cloud execution, OCR, video understanding or device control.
- The V7.2.4 package remains the formal rollback baseline. This QA package is parallel-installed and does not replace it.

## Verified On Air3

- QA package version is `7302 / 7.3.1-voice-enabled`; the V7.2.4 rollback baseline remains installed separately.
- AIKit offline wake is enabled in the installed package. The APK contains the AIKit arm64 native library and IVW resources.
- Device-private keyword file is present and contains `小叮当;`.
- Cold-start logs confirm AIKit authorization `code=0`, IVW resource loading and `AIKit wake session started`.
- The app process remains foreground after wake initialization; no new `AndroidRuntime` crash was observed in the deployment check.

- Expert collaboration opens to the remote collaboration page with a bottom safe-area hangup action.
- F10 exit returns to the unified standby HUD.
- Six seconds after expert exit, `dumpsys media.camera` reports no active camera client and `Device 0 is closed, no client instance`.
- `logcat -b crash` was empty after the exit flow.
- Public expert service health check returned `{"ok":true,"service":"expert-collab"}` from `https://bb.chinacedar.top:2305/health`.

## Fresh Automated Evidence

- Android debug unit tests: 48 tests, 0 failures, 0 errors.
- Android debug assembly: passed using a temporary `unit-test-only` key; the generated generic debug APK was not installed or released.
- `npm run validate:native-hud`: passed.
- `npm run validate:native-build`: passed.
- Expert collaboration service tests: 21 passed.
- Expert collaboration web tests: 28 passed.
- `git diff --check`: passed.

## Release Boundary

This is a QA verification snapshot, not a formal V7.3 release. Building the formal package requires the approved Direct AI, ASR and offline wake environment variables. Those values were intentionally not written to source, artifacts or this document.

## Remaining Human Acceptance

- Real wearer wake-word accuracy for "小叮当".
- Natural speech transcription and multi-turn image diagnosis quality.
- Bidirectional TRTC audio/video quality with a real expert.
- Weak-network recovery, 20-minute stability and thermal behavior.
