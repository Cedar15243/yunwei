# 叮当 AI 运维眼镜 V9.0.0 正式发布说明

发布日期：2026-08-08

验证更新：2026-08-13

## 发布身份

- `applicationId`: `com.codex.air3nativecamera.dingdangexpert.v9`
- `versionCode`: `900000`
- `versionName`: `9.0.0`
- 运行时：服务端受管、失败关闭；V9 与稳定 APK 并存，不覆盖稳定包。

## 已交付能力

- 项目、任务、结束摘要、项目记忆和项目指令的服务端上下文隔离；历史项目导航只浏览记录，不自动恢复活跃任务，V9 恢复必须从受管项目记录发起并经过二次确认与服务端刷新。
- 结束旧任务后的新任务不继承旧任务对话；人工确认的项目记忆继续保留。网关强制聊天会话与 `localTaskId` 一致，异步结束回执只收口目标旧任务，不影响随后创建的新任务。
- Supabase 权威项目 UUID、不可变网关项目映射、后台 outbox 同步，以及项目/成员原子创建；跨组织同名项目、关闭项目和撤权成员均失败关闭。
- 小叮当唤醒与声纹监听两种互斥语音模式，统一音频采集协调和抢麦释放。
- 服务端 Skill/知识版本、授权、执行上下文与工作流签名推送；知识附件支持私有 Storage、版本历史、签名下载，以及 TXT/Markdown 本地解析和 PDF/DOCX 私有服务端真实解析。
- 工作流照片/视频证据支持持久化分片上传、断点续传、分片/总摘要校验、幂等完成、显式取消和可恢复的临时分片清理；取消后拒绝旧分片/完成请求，退出工作流不会继续上传，部分取消状态可在下次 session 自愈，旧的一次性上传接口保留兼容。
- 管理 Web 的设备、账号/身份、项目范围授权、Skill/知识、工作流、维修工单只读能力、任务记录、项目记忆、媒体中心、声纹、审计和独立“系统设置”入口；项目记忆只展示云端人工确认的结束记录，并可进入真实任务时间线。
- 账号邀请通过 Supabase Auth Admin 邮件完成，角色调整通过受控 RPC 完成；客户端不接收、生成或保存密码，邀请或审计失败会回滚并明确报错。
- MVS 工单详情、SOP、节点表单和照片证据本机草稿；MVS 未提供正式写回协议时明确拒绝写入。
- 原有专家协同、AI 对话、Camera2 和稳定 APK 保持隔离。

## 固定模型合同

- 主 AI/视觉：`qwen3-vl-plus`
- 实时 ASR：`fun-asr-realtime`
- 小叮当唤醒：上一版讯飞 AIKit
- 声纹录入和 1:1 验证：讯飞新版 `s1aa729d0`
- V9 不使用 OpenClaw、GPT-5.x、DeepSeek、Claude、Qwen Max 或未经批准的新模型。

## 本地验证

- Android JVM：`96` 个报告、`563/563`，0 失败、0 错误、0 跳过；本轮使用 Temurin JDK 17、上一版讯飞 AIKit 与 Sherpa 依赖新鲜执行。
- Android 工具链已升级为 AGP `8.2.2` + Gradle `8.2` + JDK 17，直接支持 API 34；原 API 34/D8 与 Kotlin 元数据兼容提示已消除，不再依赖 suppress 配置隐藏警告。
- 管理 Edge Function：管理路由 `55/55`、设备记忆 `3/3`；覆盖记录中心、项目记忆、设备健康、设备目录登记/编辑、账号邀请、角色变更、身份恢复、项目授权和失败回滚。
- Supabase Edge Function 全量：`250/250`；Deno `index.ts` 类型检查通过。
- 管理 Web：`25` 个测试文件、`133/133`；TypeScript 检查和生产构建通过。
- 管理 Web 真实浏览器：任务筛选、媒体切换、失败证据重试、项目记忆和任务时间线通过；`1440x900` 与 `390x844` 无横向溢出，控制台 0 错误、0 警告。
- 管理 Web 生产部署已改为 `stage -> activate -> rollback` 两阶段合同：不可变 image/image ID、构建配置 SHA-256、独立 `127.0.0.1:8788`、只读根文件系统、最小 capabilities、candidate/current/previous 原子指针和管理站/专家/V9 三服务健康门禁。2026-08-12 已在目标云服务器完成无端口临时容器冷构建与 `/health` 验证；专家协同、Caddy 容器及 Caddyfile 验证前后未变化，临时资产已清理。该结果不等于生产上线，当前没有管理 Web 正式容器或 `8788` 监听。
- V9 网关 Python：`165/165`；覆盖任务会话错配拒绝、关闭旧任务后新任务对话隔离、项目持久记忆继续生效，以及数据库/evidence 媒体备份恢复。
- Supabase、管理 Web、工作流、Skill/知识、Native HUD/Build 和 AI brain 合同验证通过。
- 正式 APK SHA-256：`02DABEE95134D8FA9376F415880E08F7AA2872BC43BB2BDE92CA51BCF05806E5`；正式交付 ZIP 的最终 SHA-256 只以输出目录同名 `.sha256` sidecar 为准，避免包内文档形成自引用旧值；已完成包名、版本、v2 签名、禁用模型和长期密钥扫描。
- 能力中心中原先含混的占位路由已收口为 `Route.OPERATION_DETAIL`；Android 全量回归仍为 `563/563`，Air3 点击“巡检任务”可进入四类真实任务目录，不再显示本地内容占位提示。
- 正式交付 ZIP 由 `SHA256SUMS.txt` 和 `DELIVERY_MANIFEST.json` 校验；产物数量读取交付包内清单，ZIP SHA-256 只以输出目录同名 `.sha256` 文件为准，避免文档自引用造成哈希失真。
- 正式交付包同时包含 CycloneDX 1.5 SBOM（`309` 个组件）和依赖审计报告；npm 根工具链、管理 Web 运行时、Python 网关均为 0 个已知漏洞，审计门禁失败时不生成 ZIP。
- 正式交付包新增 V9 网关数据库与 `evidence/` 媒体备份恢复工具、每日备份 timer、临时目录恢复演练、`/ready` 就绪探针、Prometheus 告警规则和独立 `dingdang-v9-monitor` 定时监控单元；后者监测本机/公网 V9、专家健康、备份 timer 与备份新鲜度，不配置 webhook 时明确只做本机审计。
- 2026-08-13 已在授权云服务器完成正式隔离恢复演练：两份 SQLite `quick_check=ok`、证据文件 `3/3` 恢复、备份 manifest 校验一致，V9 本机/公网 `health/ready` 与专家 `/health` 均正常；生产数据、V9 指针、专家服务和 Caddy 未改写。独立监控 release 已启用并首次完成 7 项成功检查。真实通知 webhook、通知演练、磁盘满/云端依赖故障演练仍保持 pending。
- 2026-08-12 已对授权生产目标 `https://bb.chinacedar.top:2305` 执行 OWASP ZAP `2.17.0` 被动 DAST r4；`/v9-ops` 范围内 High/Medium/Low 风险数均为 0，仅有对 `Cache-Control: no-store` 的信息级复核。该证据已随正式交付包提供并由打包器、仓库校验器和包内自校验器复核；第三方渗透、独立安全审批和市场 GA 外部门禁仍保持 `pending`。
- 正式交付包携带 `verification/air3/` 脱敏实机证据：巡检与首页截图、必要 UI 树和与当前 APK SHA-256 绑定的本地验证清单；原始 logcat 不进入交付包。
- 正式交付包携带 `verification/air3-soak/summary.json` 与 `AIR3_HARDWARE_SOAK.md`：当前正式耐久为 `1203 s`、41 次采样、Camera2 `3/3`，无 Camera2/音频泄漏、Crash 或 ANR；摘要已脱敏并绑定当前 APK。该证据全程 USB 供电，不能据此宣称电池续航或真实功耗已放行。
- 正式交付包携带只读 `security/verify-v9-delivery.ps1`；收件人解压后可独立校验内部 `SHA256SUMS`、完整文件集合、交付清单、固定模型合同、正式 APK 绑定和 Air3 耐久语义。即使重新计算清单与哈希，时长、Camera2、热状态、PSS、jank、供电边界、泄漏、V8 或隐私字段失真仍会失败关闭。
- 交付包附带 `COMPLETION_MATRIX.md`，逐项区分本地已证明能力、真实环境待验收项和 MVS 协议阻断项，不把候选版误标为市场 GA。
- Air3 正式 V9 当前交付 APK SHA-256 为 `02DABEE95134D8FA9376F415880E08F7AA2872BC43BB2BDE92CA51BCF05806E5`；本轮正式耐久 `1203 s / 41 samples`，Camera2 `3/3`，无音频/相机泄漏、Crash 或 ANR，V8 未变化。设备验收仍不等同于市场 GA：双语音、声纹本人/旁人、弱网恢复、真实功耗和温升仍需外部条件齐备后执行。

## 外部放行条件

本文件是正式候选版交付说明，不把本地测试或云端隔离容器合同等同于市场 GA。Supabase CLI 登录与浏览器公开配置、独立管理域名 DNS、生产部署权限、MVS 正式上传/附件/Form/操作协议、Air3 设备激活与可用受管服务、讯飞 AIKit 用途审批及真实双语音/声纹/弱网/功耗温升验收完成前，生产状态必须保持待外部验收，所有缺失能力继续失败关闭。
