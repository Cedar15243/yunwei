# V7.3.18 连续唤醒修复 QA 记录

**日期：** 2026-07-24
**包名：** `com.codex.air3nativecamera.dingdangexpert.follow.preview.v734taskqa`
**版本：** `7318 / 7.3.18-voice-wake-rearm-qa`
**状态：** 已构建，未安装到 Air3。

## 修复范围

- 修复 AI 运维对话页执行“下一页”等本地语音命令后，再说“小叮当”没有反馈的问题。
- 根因是 ASR 完成后语音流停留在 `FINAL_READY`，离线唤醒调度只允许 `IDLE`，导致重新调度被立即拒绝。
- 本地命令处理完成且没有录音或 AI 请求时，统一将语音流归还到 `IDLE`，再恢复离线唤醒。
- 拍照、AI 请求和专家通话仍由现有麦克风所有权条件保护，不会被离线唤醒抢占。

## 验证证据

- 新增流程门禁，并确认其在修复前失败、修复后通过。
- Android 单测：`69/69` 通过。
- `npm run validate:native-hud`：通过。
- `npm run validate:native-build`：通过。
- Direct AI + Offline Wake `assembleDebug`：通过。
- APK v2 签名：通过。
- SHA256：`D0A14D8EA57802D6ED63C50D4B336DE1A1DF77A9F862399DF6A05B2B2A6E3C39`。

## 真机边界

- Air3 当前仍安装 `7316 / 7.3.16-air3-thermal-stability-qa`，本轮未安装新包。
- 安装 7318 后必须真人复测：进入 AI 运维对话 -> 当前摘要 -> “小叮当，下一页” -> 再说“小叮当”，确认提示音、页面反馈和后续命令连续可用。
