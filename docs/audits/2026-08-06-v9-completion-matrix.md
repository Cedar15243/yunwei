# V9 完成度与放行矩阵

审计日期：2026-08-12

本矩阵按 V9 市场版完整功能规格逐项核对当前工作树、自动化输出、正式 APK、Air3 实机和外部前置条件。`已证明` 只表示当前证据足以支持该项；`外部验收` 表示代码和本地合同已完成，但不能用 fixture、静态文案或健康端点代替真实环境。

| 领域 | 当前结论 | 证据或剩余条件 |
| --- | --- | --- |
| 稳定包隔离与模型合同 | 已证明 | V9 独立 `applicationId`、正式签名、上一版 AI/ASR/唤醒模型、讯飞 `s1aa729d0` 声纹；发布扫描拒绝 OpenClaw、未批准模型和长期密钥。 |
| 账号、角色、项目授权 | 本地已证明，生产待验收 | Auth Admin 邀请、角色 RPC、项目范围和失败回滚均有 Edge/Web 测试；真实登录邮件、RLS 和多角色账号需生产凭据。 |
| 设备激活、绑定、短期会话 | 本地已证明，生产待验收 | 设备注册/撤销/会话/MDM 合同和 Android 客户端已通过；Air3 当前未激活，不能宣称线上激活闭环。 |
| 两种语音模式与抢麦 | 本地已证明，真人验收待完成 | 互斥状态机、双击策略、相机/录像/专家释放和不自动恢复均有 JVM/契约测试；Air3 已完成未激活状态下 20 分钟 Camera2/待命耐久，但真实双语音、噪声、声纹、抢麦、功耗和温升仍需现场服务可用后验收。 |
| 声纹录入与 1:1 验证 | 本地已证明，供应商/真人验收待完成 | 授权、三段录入、重录、删除、锁定、撤销、反重放和审计已实现；真实讯飞服务、本人/旁人、噪声误受误拒和 p95 数据需外部验收。 |
| 语音命令与高风险确认 | 已证明本地合同 | 菜单命令优先、普通内容进入 AI、全局命令、高风险二次确认和模式帮助有 Android/Web 测试；真实 ASR/声纹链路仍受设备激活条件约束。 |
| 项目、任务、结束、恢复、记忆 | 已证明本地合同 | 任务隔离、结束摘要、项目指令、服务端 ExecutionContext、历史显式恢复、旧任务回执隔离和任务/项目时间线已通过网关/Android/Web 测试。 |
| Skill 与知识库 | 已证明本地合同，生产授权待验收 | 草稿、审核、发布、撤销、回滚、设备 manifest、私有附件解析和 AI 上下文注入已通过；真实 Supabase Storage、解析凭据和多角色授权需生产 smoke。 |
| 可配置工作流 | 已证明本地合同，云端推送待验收 | DSL 白名单、签名版本、工单绑定、自动推送、眼镜执行、动态表单、照片/视频证据、离线队列和终态收口已通过；真实签名公钥投放、在线推送和多设备需云端验收。 |
| 维修工单一期 | 本机草稿/只读已证明，写回明确阻断 | 任务列表、详情、SOP、当前节点表单、签到签退、流程记录和照片草稿可执行；MVS 正式上传、附件绑定、完整 Form schema、操作字典和 DTO 未提供，禁止伪造提交成功。 |
| AI 对话与证据 | 已证明本地合同 | ExecutionContext、项目记忆、图文/转写配对、自然回复、分页与维修步骤分离、错误恢复和本地证据持久化已覆盖；真实模型服务与现场语义准确率需外部验收。 |
| 媒体上传与同步 | 本地协议已证明，生产 Storage 待验收 | 分片校验、断点续传、取消、幂等、临时分片清理和短期 URL 已由 Python/Android/Edge 覆盖；真实断网、大视频、RLS 和对象清理需生产 smoke。 |
| 专家协同 | 原有链路回归已证明，现场联调待验收 | 专家服务端 `31/31`、Web `39/39`，V9 不改原站点；真实 Air3 双向音视频、退出回原步骤和音频抢占需在线设备验收。 |
| 管理 Web 与云端部署 | 云端隔离合同已证明，生产激活/登录待验收 | 当前 `133/133` 通过；设备记忆目录页面支持真实登记/编辑、`GET/POST/PUT /management/equipment`、固定确认词、原因、幂等键和乐观锁；typecheck、生产构建和 `1440x900`/`390x844` 真浏览器回归通过。2026-08-12 已在目标云服务器以无端口、只读根文件系统、最小 capabilities 的临时容器完成真实 Docker 构建和 `/health` 校验，专家协同、Caddy 指纹及 Caddyfile 哈希前后不变，临时资产已清理。正式 `stage/activate/rollback`、隔离 `8788`、image ID、配置摘要和三服务健康门禁已进入交付合同；Supabase CLI 仍 `Unauthorized`，且独立管理域名没有可用 A 记录，所以尚未创建生产配置、candidate/current、Caddy 站点块或管理 Web 正式容器。 |
| 设备记忆授权目录 | 本地已证明，生产数据待验收 | 眼镜端 `GET /device-sync/device-memory` 与管理端设备目录接口均失败关闭、按设备/项目权限过滤、禁止本地假数据；管理写操作通过 `manage_ops_equipment` 审计事务 RPC 原子登记/编辑并回读授权 DTO；真实 `ops_equipment` 数据和生产 RLS 仍需 Supabase smoke。 |
| 设备目录管理写操作 | 本地已证明，生产账号待验收 | 组织管理员可登记、编辑并替换项目绑定；操作原因、`CREATE_EQUIPMENT`/`UPDATE_EQUIPMENT`、幂等键、`expectedUpdatedAt`、稳定 409/404/403/400/503 映射均有 Edge/Web 合同和真实点击证据；生产管理员账号、多角色 RLS 和审计落库仍需外部 smoke。 |
| 安全供应链 | 已证明 | CycloneDX 1.5 SBOM `309` 组件；npm 根工具链、管理 Web 运行时、Python 网关依赖均 0 已知漏洞；正式包门禁强制校验 SBOM、审计和秘密扫描。 |
| 备份恢复与 SRE 运行控制 | 本地控制已证明，生产演练待验收 | 网关新增带 SHA-256/SQLite quick-check 的双库与 `evidence/` 媒体备份恢复、每日 systemd timer、临时目录恢复演练、`/health`/`/ready` 探针和 Prometheus 告警合同；真实磁盘故障、生产恢复、通知链和 RPO/RTO 演练仍待云端。 |
| SAST/DAST 与渗透测试 | 开发方被动 DAST 已证明，第三方测试待执行 | 2026-08-12 对授权目标 `https://bb.chinacedar.top:2305` 执行 OWASP ZAP `2.17.0` r4；报告仅包含 `/v9-ops` 范围实例，High/Medium/Low 风险数均为 0，信息级项为预期的 `Cache-Control: no-store` 复核；原始 JSON/HTML、目标范围和语义校验已纳入正式交付包。该结果不是第三方渗透、独立安全审批或复测报告，市场 GA 继续阻断。 |
| 外部验收证据门禁 | 已证明本地合同，真实证据待录入 | 生产部署与市场 GA 使用 `evidence/v9-external-acceptance/manifest.json` 绑定当前 APK/ZIP；九类 gate 均要求结构化 attestation 和完整必检项，并校验生产环境、完成时间、文件范围、字节数和 SHA-256。审批使用固定哈希的 Ed25519 公钥信任库：生产需发布负责人签名，市场 GA 还需独立安全审批签名；缺项、失败项、篡改、伪造签名、信任库错绑、版本错绑、早于发布或路径穿越均失败关闭。 |
| Air3 硬件/系统非回归 | 当前证据已证明相机、专家协同和未激活耐久基线 | 正式 APK 与设备 APK SHA-256 均为 `02DABEE95134D8FA9376F415880E08F7AA2872BC43BB2BDE92CA51BCF05806E5`；正式 V9 安装器已证明动态 launcher、前台启动、空 Crash buffer 和 V8 `audit48` 不变。当前正式耐久为 `1200/1203 s`、`41/41` 次采样、Camera2 `3/3`，CPU/GPU/skin 峰值 `50.9/49.2/48.622°C`，PSS `98827 -> 98827 KB`、增量 `0 KB`，jank `3.03%`，无 Camera2/音频泄漏、Crash 或 ANR；专家协同取消、确认进入等待页和挂断返回原入口已通过。设备全程 USB 供电，`powerMeasurementValid=false`，真实双语音、声纹、弱网、长时佩戴、续航和功耗仍待服务激活后验收。 |

## 放行结论

- 本地候选版：通过，可构建、可回归、可审计、可交付；Android JVM 当前为 `96` 个报告、`563/563`。
- 生产部署：管理 Web 云端隔离容器合同已通过；仍待 Supabase 登录与公开配置、真实账号/RLS、设备激活、独立管理域名 DNS 和云端正式激活。
- MVS 写操作：待正式协议；协议缺失期间保持只读/本机草稿和失败关闭。
- 市场 GA：暂不放行，必须完成上述外部验收，不得把本地证据扩大解释为线上闭环。
