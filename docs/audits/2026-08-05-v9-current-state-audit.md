# V9 当前状态与发布前审计

审计日期：2026-08-05

审计对象：`codex/v9-ops-platform` 当前工作树、V9 Android、V9 网关、Supabase Edge Function、管理 Web、工作流/Skill/知识控制面、MVS 维修工单适配层和正式交付包。

## 结论

本地工程已达到“可构建、可回归、可交付”的正式候选版本；尚未达到“已完成生产上线和 Air3 实机市场验收”。所有未具备真实外部条件的能力继续失败关闭，不用 mock、fixture 或静态成功文案冒充完成。

## 已由当前证据证明

| 范围 | 证据 |
| --- | --- |
| 模型合同 | 主 AI/视觉 `qwen3-vl-plus`；实时 ASR `fun-asr-realtime`；上一版讯飞 AIKit 唤醒；讯飞 `s1aa729d0` 声纹。V9 构建拒绝非白名单模型。 |
| Android 正式包 | `com.codex.air3nativecamera.dingdangexpert.v9`，`900000 / 9.0.0`，正式签名、v2 校验、无长期供应商密钥，V8 包名不覆盖。 |
| 眼镜运行时 | 项目/任务隔离、项目记忆、结束摘要、Skill/知识执行上下文、双语音模式、统一音频协调器、工作流推送/执行和本机证据草稿均有源码与 JVM/契约测试。历史导航只浏览项目，不自动恢复 V9 活跃任务；异步结束回执只完成目标旧任务。 |
| 云端控制面 | 短期设备会话、身份/设备授权、账号邀请/角色 RPC、Skill/知识版本与授权、工作流签名/绑定/推送、声纹生命周期、MVS 只读 DTO 和失败关闭策略均有 Edge Function/Python 测试。 |
| 管理 Web | 25 个测试文件、122/122 通过，TypeScript 和生产构建通过；任务记录具备搜索/筛选/稳定分页/受控 CSV 导出，项目记忆只展示人工确认的云端结束记录并可进入任务时间线，媒体中心使用独立分页接口、短期签名 URL、精确失败原因与管理员最小重试。工作流、工单、Skill/知识、设备、账号生命周期、项目授权、声纹、审计和系统设置等入口均使用真实 API 契约，不提供本地假激活。 |
| 真实浏览器 | 记录中心任务筛选、媒体切换、失败证据重试、项目记忆和任务时间线通过；`1440x900` 与 `390x844` 均无横向溢出，最终控制台 0 错误、0 警告；保持现有 Product Design 工作台结构。 |
| 专家协同隔离 | 服务端 31/31、Web 39/39；现有专家站点健康检查 HTTP 200，V9 未修改其端口、路由或部署。 |
| 自动化回归 | Android JVM `548/548`；V9 网关 Python `155/155`；管理 Edge Function `48/48`；Supabase Edge Function `240/240`；管理 Web `122/122`。Supabase、native HUD/build、AI brain、workflow、Skill/knowledge 和 V9 release 门禁通过。 |
| 交付包 | 当前源码重新生成 `DELIVERY_MANIFEST.json`、`SHA256SUMS.txt` 和正式 ZIP；产物数量以清单为准，ZIP SHA256 以同目录 `DingdangAI-V9-9.0.0-formal-delivery.zip.sha256` 为准。 |

## 仍需真实外部验收

1. Supabase project ref、登录令牌、生产 secrets、真实资产标签和合规远程探针；当前 `canDeployNow=false`，因此不能宣称云端已部署。
2. MVS 正式 HTTPS 测试环境、工程师最小权限凭据、完整状态/操作字典、Form schema、上传完成和附件绑定协议；在协议确认前，照片和表单只保持本机草稿。
3. Air3 `YM00FCF3NW0031` 已并存安装 V9，并取得 Camera2 打开/释放、前后台、稳态性能、Crash/ANR 和 V8 未覆盖证据；设备未激活且受管服务不可用，真实双击声纹、本人/旁人验证、抢麦、弱网、耗电和温升仍未取得放行证据。
4. 讯飞 AIKit 原厂 AAR 的正式用途/安全审批，以及声纹真实三段录入、独立 1:1 验证和噪声/重放门槛数据。
5. 服务器 SSH 22 端口和生产部署权限；当前不能从工作区直接替换云端版本。

## 发布判定

- **本地正式交付：通过。**
- **生产部署：待外部凭据与权限。**
- **市场 GA：暂不通过，必须完成上列真实外部验收后再放行。**

本审计不把本地测试、历史截图、模拟数据或健康端点单独当成市场闭环证据。
