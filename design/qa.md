# V9 Workflow Studio 设计 QA

原型：`http://127.0.0.1:5189/`

依据：`design/context.md`、`docs/superpowers/specs/2026-08-01-v9-configurable-field-app-workflow-design.md`、`docs/superpowers/plans/2026-08-01-v9-workflow-studio-web.md`

日期：2026-08-01

## 视觉

- PASS：`1440x900` 登录门禁保持白、雾灰、绿色运维工作台视觉，登录面板为 `440x521`，完整位于视口内。
- PASS：`1024x768` 登录门禁无横向或纵向异常溢出，登录面板为 `440x521`，完整位于视口内。
- PASS：标题、说明、标签、输入框和登录按钮无重叠、裁切或文字越界。
- PASS：没有新增卡片嵌套、装饰性渐变球或另一套视觉语言。

## 行为

- PASS：页面标题为“叮当 AI 运维管理平台”，真实 Supabase 登录门禁可达。
- PASS：邮箱输入框可聚焦和输入，桌面与窄屏均未出现控制台错误或警告。
- PASS：未使用假认证、假工作流数据或客户端伪造成功绕过真实登录门禁。
- 未验证：登录后的现场应用目录、画布、节点配置、保存、服务端校验、发布、版本和工单绑定浏览器链路需要真实授权账号。

## 需求

- PASS：工作流编辑器自动化覆盖受控节点、固定 HUD 预览、脏状态、真实保存、服务端校验、发布确认、不可变版本和工单解析。
- PASS：正式入口不包含任意 HTML、脚本、URL、HTTP 节点或客户端凭据配置。
- 未验证：真实 Supabase 数据、生产签名密钥、在线推送、Android 运行时和 Air3 执行不属于本次 Web 本地 QA 已完成范围。

## 可访问性

- PASS：登录输入框具有可访问名称，登录按钮可由语义角色识别。
- PASS：键盘焦点样式由现有控件合同提供。
- 待人工验证：完整键盘顺序、屏幕阅读器播报和生产环境对比度仍需登录后的真实页面验证。

## 结论

阶段结论：`CONDITIONAL READY`。本地 Workflow Studio 代码与登录门禁可以提交；因缺少真实管理账号，不能把登录后的浏览器流程标记为验收完成，更不能把本地结果表述为云端或市场版本已交付。

## V9.0.16 眼镜菜单与受管入口回归

日期：2026-08-03

- PASS：Air3 `1920x1080` 首页布局、HUD 比例、色彩和九宫格九项能力保持原确认版，没有新增页面体系。
- PASS：三道杠第一次打开能力中心，第二次关闭并回到首页。
- PASS：仅 V9 安全运行时显示“项目记忆”和“设置”两个紧凑入口，没有替换或挤压九宫格能力。
- PASS：“项目记忆”真实加载服务端 5 个项目，支持两页分页并可返回能力中心。
- PASS：“设置”复用现有任务详情 HUD 模板，进入“声纹与语音模式”，显示受管声纹 `录入中 · 0/3`。
- PASS：V8.0.48 可独立启动；V9.0.16 没有覆盖原稳定包，crash buffer 为空且无 ANR。
- 未验证：本人三段声纹录入、独立第四段 `1:1`、双击监听和音频抢占必须使用本人实时声音与真实现场条件完成。

证据：`docs/verification/2026-08-03-v9-managed-menu-air3.md`。

## V9 Skill/知识治理 Web 回归

日期：2026-08-03

- 依据：`design/context.md` 和 V9 Skill/知识版本、审核、发布、分配、授权及设备清单合同。
- PASS：真实登录门禁仍为正式入口；本轮只使用不写仓库、无外部副作用的组件 QA 环境检查登录后界面，没有加入假认证或生产预览开关。
- PASS：`1280x720` 下 AI 运维技能、华方知识库、版本元数据、授权记录和高影响操作保持白、雾灰、绿色高密度工作台结构，无横向溢出或控制台错误。
- PASS：知识草稿仅提供内部手册、完成案例和外部链接三种当前可提交来源；不存在未接通的“上传文件”入口。
- PASS：`uploaded` 状态只显示“等待后台安全扫描”，不存在可伪造 Worker 成功的“开始扫描”按钮；加载、空数据和失败重试状态均明确可见。
- PASS：`1024x768` 下 Skill/知识治理页解除历史 `1080px` 最小页宽限制，页面 `scrollWidth` 与 `clientWidth` 一致，右侧授权和撤销操作不再被截断；其他管理模块布局未改。
- PASS：管理 Web 全量 `64/64`、TypeScript 检查和生产构建通过；Edge Function `142/142`、`deno check` 与 Supabase 合同校验通过。
- 未验证：真实 Supabase 登录后的组织数据、五角色负向权限、迁移/Function/管理 Web 生产部署和真实设备清单联动仍需云端凭据与部署证据。

结论：Skill/知识治理 Web 本地阶段 `READY`；云端发布阶段仍为 `NO-GO`，不得把组件 QA 或本地合同测试表述为线上可用。

## V9 声纹中央管理 Web 回归

日期：2026-08-03

- 依据：V9 声纹生命周期、账号/设备绑定、管理员权限、二次确认、审计和现有管理平台视觉合同。
- PASS：主导航新增独立“声纹管理”入口，未改变其他导航、页面布局或现有白、雾灰、绿色工作台视觉。
- PASS：页面展示人员与设备映射、讯飞声纹状态、`3/3` 录入进度、连续失败次数、授权版本、最近验证和审计链状态，不展示讯飞密钥、模板引用或录音。
- PASS：超级管理员撤销需要原因和二次确认；成功后状态更新为“管理员已撤销”且撤销入口消失；失败时对话框、原因、错误提示和重试操作保留。
- PASS：`1440x900` 与 `1024x768` 均无横向溢出，`body min-width` 为 `0px`；`1024x768` 撤销对话框完整位于视口内，页面与控制台无错误或警告。
- PASS：管理 Web `78/78`、TypeScript、生产构建，Edge Function `154/154`、`deno check`，V9 网关 `102/102` 和 Supabase 全合同校验通过。
- 未验证：真实 Supabase 登录、迁移、Function、两项声纹管理代理 secrets、管理 Web 云端发布，以及本人三段录入和独立第四段 `1:1` 仍需生产环境与 Air3 现场证据。

结论：声纹中央管理本地阶段 `READY`；生产发布阶段仍为 `NO-GO`。

## V9.0.25 任务结束记录 Air3 回归

日期：2026-08-03

- PASS：任务结束摘要复用既有白、雾灰、绿色 HUD，首页、顶栏、能力中心和专家协同视觉未改。
- PASS：摘要按自然信息分页为 3 页，页码、上一页/下一页、完成/关闭切换、确认按钮和底部语音提示无重叠、遮挡或文字越界。
- PASS：专家协同退出后返回同一结束摘要、同一页码和原维修步骤；取消结束后继续原任务。
- PASS：人工确认成功后返回原首页 HUD，明确显示“任务已结束，项目记录已保存。下一次输入将创建新任务。”。
- PASS：完成任务不能恢复，下一次输入创建新的项目和任务，不带旧上下文。
- PASS：V8.0.48、V9.0.23、V9.0.24、V9.0.25 并存且可独立启动；目标包无 crash/ANR。
- 未验证：本人声纹三段录入和独立 `1:1`、长时监听性能、真实生产 Skill/知识授权、签名工作流推送和 MVS 工单联调。

证据：`docs/verification/2026-08-03-v9-task-end-record-air3.md`。

## V9 管理云端统一审计与系统运行回归

日期：2026-08-03

- 依据：`design/context.md`、V9 固定模型合同、真实审计脱敏合同、设备内容清单状态合同和现有管理工作台视觉。
- PASS：主导航新增“统一审计”和“系统运行”，未改变既有导航顺序、页面结构、配色、字号体系或远程专家网页。
- PASS：系统运行页明确锁定主 AI/视觉 `qwen3-vl-plus`、实时 ASR `fun-asr-realtime`、小叮当上一版讯飞 AIKit、声纹讯飞 `s1aa729d0`；只显示受控集成是否配置，不显示密钥或令牌。
- PASS：统一审计页覆盖成功、空数据、动作/对象筛选、游标加载和服务错误重试；受控浏览器查询记录证明筛选条件只在点击查询时提交。
- PASS：`1440x900` 和 `1024x768` 下导航完整可见；运营页局部解除历史 `1080px` 最小宽度，审计记录在 `<=1080px` 切换两列。最终两个视口均无横向溢出、视口外控件、文字遮挡、页面异常或控制台错误。
- PASS：管理 Web `87/87` 与生产构建、Edge 管理路由 `26/26`、`deno check`、Supabase 全合同、V9 网关 `109/109` 和上一版模型发布门禁通过。
- 说明：浏览器布局与状态测试使用受控数据，截图位于 `output/ops-management-web-qa-20260803/`；这些证据不代表真实 Supabase 生产数据或云端部署已经完成。
- 未验证：真实 Supabase 登录后的组织审计/清单数据、五角色负向权限、OIDC/SSO、迁移/Function/管理 Web 生产部署和生产告警。

结论：统一审计与系统运行本地阶段 `READY`；生产发布阶段仍为 `NO-GO`。
