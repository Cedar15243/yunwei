# Install this as a separate Caddy site block after DNS points the management
# subdomain to the cloud server. Do not add it to the existing expert site block.
{$OPS_MANAGEMENT_DOMAIN} {
    encode gzip zstd
    reverse_proxy 127.0.0.1:8788
}

